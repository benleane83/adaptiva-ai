# Copilot Instructions

## Images

- Always store image files in the `assets/images/` directory and reference them with a relative path (e.g. `assets/images/my-image.png`).
- Never use `github.com/user-attachments` URLs as image `src` values. These are ephemeral issue/PR attachment links that can break if the source issue or comment is deleted.
